{
  "name": "route-caller-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply route-caller-db --local",
    "migrate:remote": "wrangler d1 migrations apply route-caller-db --remote",
    "test": "node test/geo.test.mjs"
  }
}
