// Deployment config for the Route Caller frontend.
//
// apiBase must point at the deployed Worker in demos/route-caller/api/, e.g.
//   https://route-caller-api.<your-workers-subdomain>.workers.dev
// Leave it empty and the UI runs in preview mode with clearly-labelled sample
// data so the demo still renders. No API keys ever belong in this file.
window.ROUTE_CALLER_CONFIG = {
  apiBase: '',
};
